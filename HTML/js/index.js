"use strict";
// npm init
// npm i parcel-bundler
// npm install typescript
// sudo apt install node-typescript
// tsc
function soma(a, b) {
    return a + b;
}
console.log(soma(2, 4));
const animal = {
    nome: 'Elefante',
    tipo: 'terrestre',
    executarRugido: (alturaDb) => ($)
}, { alturaDb };
const felino = {
    nome: 'Gato',
    tipo: 'terrestre',
    visaoNoturna: true
};
